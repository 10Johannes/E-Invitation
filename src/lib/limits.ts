/** Hard cap on admin-uploaded couple photos (hero slideshow pool). */
export const MAX_COUPLE_PHOTOS = 10;

/** Maximum background music tracks the admin can configure. */
export const MAX_AUDIO_TRACKS = 20;
